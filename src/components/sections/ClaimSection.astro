---
import Icon from "astro-icon";
import { twMerge } from "tailwind-merge";

interface Props {
  id?: string;
  containerClasses?: string;
}

const { containerClasses, id } = Astro.props;
---

<!-- <section
  id={id}
  class={twMerge(
    "relative mt-6 flex items-end justify-center px-6 sm:mt-16 sm:px-10 md:px-14 lg:mt-2",
    containerClasses,
  )}
> -->
<section
  id={id}
  class={twMerge(
    "relative mt-6 justify-center flex px-6 sm:mt-16 sm:px-10 md:px-14 lg:mt-2",
    containerClasses,
  )}
>
  <div class="flex flex-row">
    <div class="basis-1/3">
      <Icon name="disegni_b_w-09" class="ml-12 hidden w-[50px] sm:block" />
    </div>
    <div class="basis-1/3">
      <div class="flex flex-col space-y-4">
        <p class="mx-4 w-96 text-center font-medium sm:mx-7">
          Seguiamo le stelle per tracciare una via nell’ignoto: ti aiutiamo a
          intercettare, comprendere, anticipare e adattarti ai cambiamenti per
          affrontare le nuove sfide globali.
        </p>
        <p class="mx-4 w-96 text-center font-medium sm:mx-7">
          Mettiamo insieme creatività e ingegneria per sviluppare soluzioni
          informatiche e tecnologiche capaci di migliorare le dinamiche
          produttive e gestionali della tua organizzazione. Progettiamo e
          realizziamo servizi, applicazioni, interfacce e laboratori per
          start-up, PMI, PA ed organizzazioni non-profit.
        </p>
        <p class="mx-4 w-96 text-center font-medium sm:mx-7">
          Progettiamo e realizziamo servizi, applicazioni, interfacce e
          laboratori per start-up, PMI, PA ed organizzazioni non-profit.
        </p>
      </div>
    </div>
    <div class="basis-1/3">
      <Icon name="disegni_b_w_02-51" class="ml-12 hidden w-[100px] sm:block" />
    </div>
  </div>

  <!-- <div
    class="absolute -right-0 inline-flex items-end lg:right-24 xl:right-20 2xl:right-40"
  >
     <Icon name="red-rectangle" class="h-5" />
    <Icon
      name="blue-rectangle"
      class="ml-8 hidden w-5 hover:text-black sm:block"
    />
    <Icon name="white-rectangle" class="ml-2 mr-5 hidden w-5 md:block" />
    <Icon name="black-rectangle" class="ml-12 hidden h-5 xl:block" /> -->
</section>
